# Note: the 'X's must come last
set suffix = "${1}-hook"
set file = "`mktemp -t virtualenvwrapper-$suffix-XXXXXXXXXX`"
if ( $? != 0 ) then
    echo "ERROR: virtualenvwrapper could not create a temporary file name." \
        > /dev/stderr
    exit 1
endif
echo $file
exit 0

